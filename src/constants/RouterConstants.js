const BASE_PATH = '/';

const ROUTE_PATHS = Object.freeze({
    EMPTY: '/',
    INDEX: '/index.html',
    SHOWS: '/shows',
    SHOW: '/shows/:id',
    EPISODE: '/episodes/:id',
});

export {
    BASE_PATH,
    ROUTE_PATHS,
};
